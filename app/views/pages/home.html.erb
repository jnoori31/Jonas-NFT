<h1>Pages#home</h1>
<div class="container">
  <div class="row justify-content-center">
    <% @nfts.each do |nft| %>
    <div class="card">
  <%= image_tag nft.image_url, height: 175, width: 300, crop: :pad”%>
  <%=nft.name %>
  <%=nft.price %>
  <%=nft.description %>
<% end %>
    </div>
  </div>
</div>

