<% if current_user %>
    <%# <div class="like-icon mx-2 <%= 'blue' if current_user.favorited?(user)" data-id="<%= user.id %>
      <%# <i class="fas fa-user-friends"></i> %>
    <%# </div> %>
    <% if current_user.favorited?(user) %>
      <i class="fas fa-user-friends"></i><p data-followUser-target="followBtn">Following</p>
    <% else %>
      <i class="fas fa-user-friends"></i><p data-followUser-target="followBtn">Follow</p>
    <% end %>
<% end %>
